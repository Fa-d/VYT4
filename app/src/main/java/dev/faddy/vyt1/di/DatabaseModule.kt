package dev.faddy.vyt1.di

/*

@Module
@InstallIn(SingletonComponent::class)
object DatabaseModule {

    @Provides
    @Singleton
    fun buildDatabase(@ApplicationContext context: Context): ChannelDatabase {
        return Room.databaseBuilder(context, ChannelDatabase::class.java, Constants.DB_NAME).build()
    }

    @Provides
    @Singleton
    fun getAppDao(appDatabase: ChannelDatabase): ChannelDao {
        return appDatabase.getChannelDao()
    }

}*/
